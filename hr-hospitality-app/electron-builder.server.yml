appId: "com.hrhospitality.server"
productName: "HR Hospitality Servidor"

directories:
  output: "dist"

files:
  - "out/**/*"
  - "electron/**/*"
  - "package.json"
  - "!electron/build_config.client.json"

extraResources:
  - from: "electron/build_config.server.json"
    to:   "electron/build_config.json"

win:
  target:
    - target: "nsis"
      arch:
        - "x64"
  icon: "public/icon-server.png"
  artifactName: "HR-Hospitality-Servidor-Setup-${version}.exe"

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "HR Hospitality Servidor"
  license: "public/license.txt"
